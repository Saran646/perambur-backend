# Environment Variables Template for Railway Deployment
# Copy these to Railway project settings

# Database (provided automatically by Railway PostgreSQL service)
DATABASE_URL=postgresql://user:password@host:port/database

# JWT Secret (generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Email Configuration (optional - only if you want email notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
ADMIN_EMAIL=admin@perambursrinivasa.com

# Admin Dashboard URL (update after deploying admin frontend to Netlify)
ADMIN_DASHBOARD_URL=https://your-admin-app.netlify.app/dashboard

# CORS Origins (update with your Netlify URLs)
CORS_ORIGIN=https://your-user-app.netlify.app,https://your-admin-app.netlify.app

# Port (Railway sets this automatically)
PORT=4000
